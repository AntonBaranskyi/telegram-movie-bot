import { KEYBOARD_MENU_ENUM } from '../types/keyboardEnum';

export const KEYBOARD_MENU = [
  [{ text: KEYBOARD_MENU_ENUM.RANDOM }, { text: KEYBOARD_MENU_ENUM.POPULAR }],
  [
    { text: KEYBOARD_MENU_ENUM.GENRES },
    { text: KEYBOARD_MENU_ENUM.ABOUT },
    { text: KEYBOARD_MENU_ENUM.FIND },
  ],
  [{ text: KEYBOARD_MENU_ENUM.CLOSE }],
];
